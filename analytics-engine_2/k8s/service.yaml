apiVersion: v1
kind: Service
metadata:
  name: analytics-engine-service
  namespace: analytics
  labels:
    app: analytics-engine
spec:
  type: ClusterIP
  selector:
    app: analytics-engine
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http