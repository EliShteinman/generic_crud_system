# requirements.txt - דרישות המערכת

# === חבילות עיקריות ===
fastapi==0.104.1              # פריימוורק API מהיר ומודרני
uvicorn[standard]==0.24.0     # שרת ASGI לייצור
motor==3.3.2                  # דרייבר MongoDB אסינכרוני
pymongo==4.6.0                # דרייבר MongoDB סינכרוני (תלות של motor)

# === חבילות לטיפול בנתונים ===
pydantic==2.5.0               # אימות נתונים ו-serialization
python-multipart==0.0.6       # טיפול בקבצים מועלים
email-validator==2.1.0        # אימות כתובות מייל

# === חבילות לניטור וביצועים ===
psutil==5.9.6                 # מידע על משאבי המערכת

# === חבילות לתאריכים וזמנים ===
python-dateutil==2.8.2        # טיפול מתקדם בתאריכים

# === חבילות לטיפול בקבצים ===
openpyxl==3.1.2               # קריאה וכתיבה של קבצי Excel
xlsxwriter==3.1.9             # יצירת קבצי Excel

# === חבילות לפיתוח (אופציונלי) ===
pytest==7.4.3                 # בדיקות
pytest-asyncio==0.21.1        # בדיקות אסינכרוניות
black==23.11.0                # עיצוב קוד
flake8==6.1.0                 # בדיקת איכות קוד
mypy==1.7.1                   # בדיקת סוגים

# === חבילות לייצור (אופציונלי) ===
gunicorn==21.2.0              # שרת ייצור
redis==5.0.1                  # מטמון ו-message broker